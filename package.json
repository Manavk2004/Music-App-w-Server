{
    "scripts": {
        "start": "concurrently \"npm run start --prefix server\" \"npm run dev --prefix MusicApp\"",
        "build": "npm run build --prefix MusicApp"
    },
    "devDependencies": {
        "concurrently": "^9.2.1",
        "rollup": "^3.29.4",
        "vite": "^4.5.3"
    }
}
